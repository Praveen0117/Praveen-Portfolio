import React from "react"
import ReactDOM from "react-dom"

const portpolio = () => {
    return(<h1>Praveen</h1>)
}

const root = ReactDOM.createRoot(document.getElementById("root"))

root.render(<portpolio />)